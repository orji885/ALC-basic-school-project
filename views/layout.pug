doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=yes ')
    meta(name='description', content='a school system that creates,edits,updates and deletes student resources.')
    meta(name='author', content='orji linda')
    title SCHOOL INFORMATION SYSTEM 
    // Bootstrap core CSS
    link(href='css/bootstrap.min.css', rel='stylesheet')
    link(href='/bower_components/bootstrap/dist/css/bootstrap.css', rel='stylesheet')
    link(href='/css/style.css', rel='stylesheet')
    // Custom styles for this template
    link(href='/css/carousel.css', rel='stylesheet')
  body
    nav.navbar.navbar-inverse
      .container
        .navbar-header
          button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
            span.sr-only Toggle navigation
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='/') BASIC SCHOOL SYSTEM
        #navbar.collapse.navbar-collapse
          ul.nav.navbar-nav
            li
              a(href='/') Home
            if user
              li
                a(href='/articles/add') CREATE AND ADD STUDENT DETAILS
          ul.nav.navbar-nav.navbar-right
            if user
              li 
                a(href='/users/logout') ADMIN LOGOUT
            else
              li
                a(href='/users/register') ADMIN REGISTRATION
              li
                a(href='/users/login') ADMIN LOGIN
    //
      Marketing messaging and featurettes
      ==================================================
    // Wrap the rest of the page in another container to center all the content.
    .container.marketing
      // Three columns of text below the carousel
      .row
        .col-lg-4
          img.rounded-circle(img='', src='/images/images-93.jpg', alt='Generic placeholder image', width='140', height='140')
          h2 FIRST STEP
          p
            | The website is only accesible by an admin, so first of all, the admin registers using the registration form.
          p
            a.btn.btn-secondary(href='#', role='button', style='background-color:blue') REGISTER
        // /.col-lg-4
        .col-lg-4
          img.rounded-circle(src='/images/images-94.jpg', alt='Generic placeholder image', width='140', height='140')
          h2 SECOND STEP
          p
            | After registration , the admin logins, without logging in,the admin cannot have access to the features of the school system.
          p
            a.btn.btn-secondary(href='#', role='button', style='background-color:blue') LOGIN
        // /.col-lg-4
        .col-lg-4
          img.rounded-circle(src='/images/images-105.jpg', alt='Generic placeholder image', width='140', height='140')
          h2 THIRD STEP
          p
            | The features include creating, editing,updating and deleting the student's name, faculty and departments.
          p
            a.btn.btn-secondary(href='#', role='button', style='background-color:blue') ACCESS THE FEATURES
        // /.col-lg-4
      // /.row
      br
      br
      hr
      .container
        != messages('message', locals)
        if errors
          each error, i in errors
            div(class="alert alert-danger") #{error.msg}
        block content
        br
        hr
        // FOOTER
        footer
          p.float-right
            a(href='#') Back to top
          p(style='background-color:black; font-size:1.5em; color:white') © 2017  ANDELA LEARNING COMMUNITY POWERED BY GOOGLE       ORJI OLUCHI LINDA
      // /.container
      //
        Bootstrap core JavaScript
        ==================================================
      // Placed at the end of the document so the pages load faster
      script(src='/bower_components/jquery/dist/jquery.js') 
      script(src='/js/popper.min.js')
      script(src='js/bootstrap.min.js')
      // Just to make our placeholder images work. Don't actually copy the next line!
      script(src='/js/holder.min.js')
      // IE10 viewport hack for Surface/desktop Windows 8 bug
      script(src='/js/ie10-viewport-bug-workaround.js')
      script(src='/js/main.js')
